# manifests/RaBbLE-NeBuLA.manifest.yml
# Visual entity renderer — the Flat-Chaos quantum visualization engine.
# NeBuLA-JS (original) is archived in RaBbLE-Chrysalis. This is the v2 rebuild.

# Identity
name: RaBbLE-NeBuLA
slug: RaBbLE-NeBuLA
description: "Flat-Chaos quantum visual renderer — the entity's ambient visual expression environment."
role: frontend

# Repository
repo: git@github.com:RaBbLE-Collective/RaBbLE-NeBuLA.git
branch: main
worktree_root: RaBbLE-NeBuLA

# Status
version: "v0.0.0.0"
epoch: 0
episode: 0
status: active
release_track: episode      # airs in Episode lockstep — tracks epoch active_branch

# Collective alignment
palette_version: "1.0"
entity_embedded: true
pulse_protocol: true

# Dependencies — other Collective members this member consumes at runtime
depends_on:
  - slug: RaBbLE-Aether
    mechanism: CDN CSS bundle (palette tokens, visual theming)

# Notes
notes: >
  Active branch: dev. NeBuLA v2 clean rebuild — no RBCNS naming constraints.
  Architecture: Canvas2dBackend owns entity rendering (particles, portals, eyes, boot sequence).
  <rabble-entity> web component lives in NeBuLA (src/element.js) — NOT in RaBbLE-World.
  Registers on World pages via the IIFE bundle loaded from CDN.
  Episode 1 deliverable: Canvas2D entity renderer at 60 FPS, CDN-ready bundle.
  Three.js Layer 2 deferred to Episode 2+.
  Original NeBuLA-JS code archived in RaBbLE-Chrysalis.
  Lore + architecture specs live in RaBbLE-Grimoire/RaBbLE-NeBuLA/.
