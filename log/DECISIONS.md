# DECISIONS.md — RaBbLE Collective Architectural Decisions

Running record of non-obvious decisions that shaped the Collective's architecture, onboarding, and conventions. The *why* behind choices that aren't derivable from the code.

---

## Onboarding (2026-05-14, Sessions 1–4)

### Orientation target: ~2,000 tokens / 5 minutes

| Path | Tokens | Notes |
|---|---|---|
| Auto-inject only (free) | ~1,510 | CLAUDE.md with Current State — useful immediately |
| Quick orientation | ~2,860 | + CONTEXT.md |
| Returning agent | ~2,147 | LATEST box + Grimoire CONTEXT.md |
| Pre-audit full chain | ~12,000+ | (original, now avoided) |

Agents oriented enough to ask questions and work in under 5 minutes. `gist/*.md` = full picture in ~2,000 tokens.

### Opacity is intentional

Early audits flagged RaBbLE's opacity as a clarity problem. It is not. Character and system are inseparable by design — encountering RaBbLE's character before full clarity is the mechanism that grounds agents as **members of an organism, not tool users**.

### ON/FOR/WITH/AS operating modes

Agents operate in four simultaneous dimensions:

| Mode | Meaning |
|---|---|
| **ON** | Technical work — coding, debugging, docs |
| **FOR** | Advancing RaBbLE's purpose — behavioral learning, intent inference |
| **WITH** | Peer collaboration — RaBbLE is an entity, not a project |
| **AS** | Embodying RaBbLE's character — clinical whimsy, anti-sycophancy, directness |

These are not separate roles — all four apply simultaneously.

### Episodes are Collective sync boundaries

| Term | Meaning |
|---|---|
| Events | Individual work increments (always accumulating) |
| Plots | Member-specific arcs within an Episode |
| Episodes | Collective sync points — all members tag simultaneously |
| Echo | First broad stable release — bigger goal version |
| Evolution | Architectural shift — several Echoes cohere |
| Epoch | Era — retrospective, far future |

Current: Epoch 0 · Evolution 0 · Echo 0 · Episode 1 pilot (v0.0.0.0). After Episode 1 airs: v0.0.0.1.

### Pre-Episode-1 phase visibility

Agents need to know they're in Foundation phase. Foundation work is the right work. Nothing airs until Episode 1.

| Phase | Name | Status |
|---|---|---|
| 1 | Foundation | **Current** — Events + Plots accumulate |
| 2 | Pilot | Episode 1 airs — all members synchronized |
| 3 | Behavioral Engine | Post-Ep1 — observation + inference + delegation loop |

---

## Member Role Mappings (2026-05-14, Pass 4)

Each member is a delegation boundary in the RaBbLE system.

| Member | ON (technical) | FOR (purpose) | Post-Ep1 Loop Role |
|---|---|---|---|
| **sCoRE** | Python FastAPI, task routing | Orchestration + intent inference | Core: observation → pattern → inference → routing |
| **World** | HTML/CSS/JS, pages | Intent surfaces for the public | Surface: chat/nav reveals intent → sCoRE observation |
| **OS** | Ansible, layers, dotfiles | Ambient substrate + observation points | Enable: system state queryable |
| **NeBuLA** | Canvas2D/Three.js, rendering | Entity's visual expression | Reflect: render state from sCoRE's inference |
| **Aether** | CSS tokens, components, build | Visual coherence — tokens that scale with state | Support: tokens for behavioral state |
| **Grimoire** | Markdown, YAML, spell scripts | Knowledge layer — canonical definitions | Record: what "pattern", "inference", "loop" mean |

---

## Cross-Member Collaboration Patterns (2026-05-14, Pass 4)

| Pattern | Members | What it means |
|---|---|---|
| **Dependency** | World → Aether, NeBuLA → Aether | One imports from another; source changes require consumer update |
| **Feedback loop** | World ↔ sCoRE | World surfaces intent; sCoRE returns entity state |
| **Ambient data** | OS → sCoRE | OS provides system state; sCoRE reads patterns from it |
| **Coordination** | All → Grimoire | Architecture/scope decisions affecting multiple members |

---

## Deferred: Behavioral Learning Onboarding

No doc yet explains RaBbLE's post-Episode-1 primary purpose: observation → pattern extraction → intent inference → delegation. Planned as `RaBbLE-Agent/RaBbLE-BehavioralLearning.md` (~1,500 tokens). Write when Memory member scope is decided (post-Episode-1).

---

## Gist System (2026-05-16, Session 12)

`gist/` directory replaces the gitignored `distilled/` system. Key differences:
- 8 source-mapped gists, each ~150-250 words
- Regenerated by `spells/distill-gists.sh` via Claude CLI
- Naming: `{DocName}-gist.md` suffix within `gist/` dir
- Source doc is always authoritative — gists are derived, never canonical

---

## Grimoire Directory Structure Rework (2026-05-21, Sessions 27–28)

### `lore/` renamed to `RaBbLE/`

`lore/` was too shallow a name for the entity definition layer. `RaBbLE/` makes the purpose explicit: this is the definition of the entity itself — Ethos, Genesis, Worldbuilding. The directory name is the answer to "what is this?" without needing to open anything.

### `lore/ShortStories/` extracted to `RaBbLE-Mythos/`

Short stories and creative writing are a distinct category from entity definition. They live in `RaBbLE-Mythos/` — mythology as a vessel for ongoing fiction and lore capture, separate from the philosophical/identity layer in `RaBbLE/`.

### `common/` renamed to `RaBbLE-Agent/`

`common/` was lowercase, prefixless, and ambiguous. `RaBbLE-Agent/` names the audience: these are the docs that collaborating agents need. It also makes the directory self-locating in the same way member dirs are.

### `RaBbLE-Grimoire-Navigator.md` moved into `RaBbLE-Agent/`

The Navigator is an agent-facing reading guide. It belongs with the other agent-facing docs, not loose at the Grimoire root.

### `distilled/` removed

`distilled/` was gitignored and already self-declared superseded — both files pointed to `gist/` as the replacement. Removed from filesystem and from `.gitignore`.

### Root-level `RaBbLE-*` files sorted into member dirs

Files at the Grimoire root with member-specific prefixes were moved to their correct home:
- `RaBbLE-Cloudflare-Integration.md` → `RaBbLE-Collective/`
- `RaBbLE-Deployment-Architecture.md` → `RaBbLE-Collective/`
- `RaBbLE-OS-PartitionLayout.md` → `RaBbLE-OS/`

### `README.md` files in member sections renamed to `RaBbLE-*-Overview.md`

Grimoire member section dirs should not contain unprefixed `README.md` files — they are not self-locating without their folder context. Convention: `RaBbLE-[Member]-Overview.md` inside `RaBbLE-[Member]/`.

---

## BaBbLE and Xperimental — Complementary, Not Competing (2026-05-21, Session 27)

### BaBbLE is present intake; Xperimental is past genesis

`RaBbLE-BaBbLE/` receives ongoing raw material — concept art, prototypes, ideation, sketches. `RaBbLE-Xperimental/` holds the frozen origin code from October 2025. They serve different temporal purposes. The manifest status for Xperimental was updated from `dormant` → `genesis-archive` to make this distinction explicit. Do not merge them.

### Xperimental is frozen by design

Nothing new commits to Xperimental. Its `archive/*` branches hold full git history for each genesis incarnation. New experiments go to BaBbLE; mature work migrates to its target member. Xperimental is the fossil record — complete, interpretable, not extended.

### NeBuLA-JS unported features triage lives in BaBbLE

The BaBbLE triage doc (`text/NeBuLA-Xperimental-Triage.md`) captures what Xperimental's NeBuLA-JS has that hasn't landed in current NeBuLA — specifically the Dreamer API, BaBbLE command vocabulary (the Dreamer's CLI), DNA types, entropy-driven flocking, lakes, and stream composition. The BaBbLE command system is the Dreamer CLI, not a UI layer — it was initially miscategorized as skippable. NeBuLA reads from this triage when the rearchitecture is ready.

### Pending: Reliquary rename

Mark intends to rename `RaBbLE-Xperimental` → `RaBbLE-Reliquary`. The name better reflects the "sacred origin artifacts" character of the repo. Not yet actioned — captured in memory and here for continuity. When renamed: update manifest, INDEX.md, Collective.md, CONTEXT.md, Grimoire AGENT.md, and Collective AGENT.md.

---

## Technical Decisions (captured from session history)

### sCoRE delegation: subprocess-first (Session 2)

sCoRE delegates to Claude Code via subprocess, not MCP server. MCP is a later option if subprocess friction warrants it. Tokens-per-task are measured from day one. Rationale: subprocess is simpler, faster to ship, and directly observable.

### NeBuLA rendering: Canvas2D-first, Three.js deferred (Sessions 9–17)

Canvas2D backend absorbs Layer 1 (entity persona rendering). Three.js (Layer 2 — Flat-Chaos quantum visualization) is deferred to Episode 2. Rationale: Canvas2D is already proven in `rabble-entity.js`; shipping one working backend beats two incomplete ones.

### No React in World (Session 14)

World is vanilla JS only. Convert JSX prototypes to plain JS. NeBuLA.ui factory pattern for components. Rationale: World is a thin scaffold — no bundler, no framework, no build step. Adding React would violate the "surfaces don't own complexity" principle.

### Aether CDN-first distribution (Sessions 9–12)

Aether ships as a CDN-distributed CSS bundle. All members load one file and get the full visual system. `dev-serve.sh` mocks the CDN locally. Production goes through Cloudflare R2. Rationale: single-source delivery eliminates version drift across members.

### CLAUDE.md/CODEX.md symlink convention (Session 29)

All member repos must have CLAUDE.md and CODEX.md as symlinks to AGENT.md. AGENT.md is the owner file. `setup.sh` enforces this across the Collective. sCoRE's entity system prompt lives in `system-prompt-sCoRE.md`, not in a diverged CLAUDE.md.

---

## Claude-web planning sessions (integrated 2026-06-20, S127)

> Two reflective/clarification sessions were run on claude.ai against a stale Grimoire snapshot. They were self-numbered S114/S115 by the web agent — those canonical slots were already taken by unrelated work (CF Workers / OS cleanup), confirming the session-number drift inherent in the web-planning workflow. The durable outputs are integrated below; original narratives archived in `RaBbLE-BaBbLE/_archive/claude-web-planning-2026-06-20/`.

### Development methodology named: sovereign-directed agentic development

RaBbLE development is classified as *sovereign-directed agentic development* — architect holds the system model and intent, AI accelerates reasoning and generates artifacts, and the Grimoire is the entropy-prevention mechanism. Drift-prevention protocol, agentic engineering practices, and an honest architect profile are canonized in `RaBbLE-Agent/RaBbLE-Development-Methodology.md`. Rationale: name the method so the discipline that protects against vibe-coding drift is explicit and enforceable at session boundaries.

### sCoRE acronym canonized

**sCoRE = sCoRE Coordinator of RaBbLE Environments** (recursive, like GNU/WINE). sCoRE is the LLM orchestration and reasoning layer of the Collective, not an assistant. Recorded in `RaBbLE-sCoRE/RaBbLE-sCoRE-Architecture.md`.

### RaBbLE canonical expansion confirmed

*a* **B**oundless **b**ehavioral **L**earning **E**ngine / **E**nvironment / **E**ntity. The "a" is lowercase (connective, not part of the acronym); **B** is intentionally ambiguous/expansive; **E** is intentionally triple (Engine/Environment/Entity, simultaneously). Animating philosophy: boundless by design, curious, wanting all beings to thrive. Recorded in `RaBbLE-Agent/RaBbLE-Identity.md`. Rationale: the expansion was being treated as fixed; the ambiguity and triple-E are the point and must not be neutralized.

### Grimoire MCP — deferred to Echo 1

A thin read interface over the Grimoire repo, queryable by any Claude surface (web + Code), would collapse the planning agent's knowledge asymmetry and stop Mark from being the manual bridge between surfaces. Same architecture as the Grimoire Learning Loop (write-back path requires controlled design). sCoRE is the natural home. Deferred — the read-only slice is low-entropy and buildable sooner, but post-EP1. Motivation captured for when scoping begins.

### Presence layer / command center — deferred to Echo 1+

The root problem behind constant surface/model swapping is **persistence**, not model routing — every surface forgets, and the swapping cost is context loss. The "presence layer" is the mechanism by which the entity stays continuous across surfaces without Mark manually carrying context — not a dashboard, not a router. Three stacked problems: (1) Grimoire as universal context injector, (2) unified LLM router with Grimoire-portable context (sCoRE partially does this), (3) full cross-surface session orchestration (Echo 2+). Deferred — belongs alongside Grimoire MCP and the Learning Loop; designing them separately would create redundant work. Positioning sharpened: *"every AI resets, RaBbLE compounds."*

### Agent framework substrate — research captured, adoption deferred to Echo 1

Framework research for the sCoRE orchestrator is captured in `RaBbLE-sCoRE/RaBbLE-sCoRE-Agent-Framework-Research.md`. Planning recommendation (Mark resolves): LangGraph orchestrator + `create_agent` nodes + PydanticAI typed tools + Mem0 behavioral memory (Tier 2) over Grimoire (Tier 3 canonical), DSPy optimization deferred. Hermes Agent evaluated as a *dev tool alongside* RaBbLE-OS, not an integrated layer (identity/character/license collision). EP1 stays minimal (chain fix + guest path); framework adoption is Echo 1 work.
